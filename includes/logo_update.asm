;
; LOGO $D016 SPLITTER UPDATE
;

; Updates the self mod for logo_split.asm

; Char line $00
		lda d016_data+$00,y
		sta logo_split_00+$01

		lda d016_data+$01,y
		sta logo_split_01+$01

		lda d016_data+$02,y
		sta logo_split_02+$01

		lda d016_data+$03,y
		sta logo_split_03+$01

		lda d016_data+$04,y
		sta logo_split_04+$01

		lda d016_data+$05,y
		sta logo_split_05+$01

		lda d016_data+$06,y
		sta logo_split_06+$01

		lda d016_data+$07,y
		sta logo_split_07+$01

; Char line $01
		lda d016_data+$08,y
		sta logo_split_08+$01

		lda d016_data+$09,y
		sta logo_split_09+$01

		lda d016_data+$0a,y
		sta logo_split_0a+$01

		lda d016_data+$0b,y
		sta logo_split_0b+$01

		lda d016_data+$0c,y
		sta logo_split_0c+$01

		lda d016_data+$0d,y
		sta logo_split_0d+$01

		lda d016_data+$0e,y
		sta logo_split_0e+$01

		lda d016_data+$0f,y
		sta logo_split_0f+$01

; Char line $02
		lda d016_data+$10,y
		sta logo_split_10+$01

		lda d016_data+$11,y
		sta logo_split_11+$01

		lda d016_data+$12,y
		sta logo_split_12+$01

		lda d016_data+$13,y
		sta logo_split_13+$01

		lda d016_data+$14,y
		sta logo_split_14+$01

		lda d016_data+$15,y
		sta logo_split_15+$01

		lda d016_data+$16,y
		sta logo_split_16+$01

		lda d016_data+$17,y
		sta logo_split_17+$01

; Char line $03
		lda d016_data+$18,y
		sta logo_split_18+$01

		lda d016_data+$19,y
		sta logo_split_19+$01

		lda d016_data+$1a,y
		sta logo_split_1a+$01

		lda d016_data+$1b,y
		sta logo_split_1b+$01

		lda d016_data+$1c,y
		sta logo_split_1c+$01

		lda d016_data+$1d,y
		sta logo_split_1d+$01

		lda d016_data+$1e,y
		sta logo_split_1e+$01

		lda d016_data+$1f,y
		sta logo_split_1f+$01

; Char line $04
		lda d016_data+$20,y
		sta logo_split_20+$01

		lda d016_data+$21,y
		sta logo_split_21+$01

		lda d016_data+$22,y
		sta logo_split_22+$01

		lda d016_data+$23,y
		sta logo_split_23+$01

		lda d016_data+$24,y
		sta logo_split_24+$01

		lda d016_data+$25,y
		sta logo_split_25+$01

		lda d016_data+$26,y
		sta logo_split_26+$01

		lda d016_data+$27,y
		sta logo_split_27+$01

; Char line $05
		lda d016_data+$28,y
		sta logo_split_28+$01

		lda d016_data+$29,y
		sta logo_split_29+$01

		lda d016_data+$2a,y
		sta logo_split_2a+$01

		lda d016_data+$2b,y
		sta logo_split_2b+$01

		lda d016_data+$2c,y
		sta logo_split_2c+$01

		lda d016_data+$2d,y
		sta logo_split_2d+$01

		lda d016_data+$2e,y
		sta logo_split_2e+$01

		lda d016_data+$2f,y
		sta logo_split_2f+$01

; Char line $06
		lda d016_data+$30,y
		sta logo_split_30+$01

		lda d016_data+$31,y
		sta logo_split_31+$01

		lda d016_data+$32,y
		sta logo_split_32+$01

		lda d016_data+$33,y
		sta logo_split_33+$01

		lda d016_data+$34,y
		sta logo_split_34+$01

		lda d016_data+$35,y
		sta logo_split_35+$01

		lda d016_data+$36,y
		sta logo_split_36+$01

		lda d016_data+$37,y
		sta logo_split_37+$01
